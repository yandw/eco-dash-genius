## 调整"双控考核结论"卡片

### 改动 `src/pages/ent/EntAssessDual.tsx`

1. **删除顶部"考核说明"提示条**（位于状态条与总体结论横幅之间，含 `Info` 图标的整块）。
2. **"双控考核结论"卡片精简为两字段 + 说明并排布局**：
   - 移除 `双控指标完成情况` 字段。
   - 卡片网格调整为：左列"考核结果"（`PassBadge`，只读），右列"备注"区块（包含考核说明文案 + 备注只读文本）。
   - 备注区块结构：
     - 顶部小字考核说明（`text-xs text-muted-foreground`，配 `Info` 图标）：
       > 考核说明：考核结果分为完成、未完成两个等次（总量和强度目标均完成可视为完成，有 1 项未完成即视为未完成）。双控指标完成情况为"未完成"但考核结果为"完成"的，需在备注中说明原因。
     - 下方"备注"标签 + `ro` 只读文本框，显示 `currentRow.remark`。
   - 高亮规则保留：当 `dualPass === "未完成"` 且 `assessResult === "完成"` 时备注框使用 `border-warning/40 bg-warning/5`。
3. `Info` 图标导入保留（仍在备注说明中使用）。

mock 数据 `dualPass` 字段保留，仍用于高亮判断；不需要改动 `assess.ts`。
